---
layout: attribute
source: W3C/HTML
$type: W3C/HTML/Attribute
$id: https://standards.org.ai/W3C/HTML/Attribute/{attribute.code}
sameAs: https://standards.org.ai/Attribute/{attribute.code}
canonical: https://w3.org.ai/HTML/Attribute/{attribute.code}
params:
  - Attribute
schema:
  attribute:
    code: name
    name: name
    elements: array
    type: string
    description: string
    required: boolean
    deprecated: boolean
    valueType: string
    defaultValue: string
    specifications: array
---

import { Header } from '@/components/standards/Header'
import { Description } from '@/components/standards/Description'
import { ElementsList } from '@/components/w3c/html/ElementsList'
import { CodeExample } from '@/components/standards/CodeExample'
import { BrowserCompatibility } from '@/components/standards/BrowserCompatibility'
import { SpecificationLinks } from '@/components/standards/SpecificationLinks'
import { DeprecationNotice } from '@/components/standards/DeprecationNotice'
import { ValueTable } from '@/components/standards/ValueTable'

<Header
  type="HTML Attribute"
  code={attribute.name}
  name={attribute.name}
  standard="W3C HTML"
/>

{attribute.deprecated && (
  <DeprecationNotice
    message={`The ${attribute.name} attribute is deprecated and should not be used.`}
    alternatives={attribute.alternatives}
  />
)}

<Description content={attribute.description} />

## Attribute Information

- **Type**: {attribute.type}
- **Value Type**: {attribute.valueType}
- **Required**: {attribute.required ? 'Yes' : 'No'}
- **Default Value**: {attribute.defaultValue || 'None'}
- **Global Attribute**: {attribute.global ? 'Yes' : 'No'}

## Applicable Elements

<ElementsList
  elements={attribute.elements}
  attributeName={attribute.name}
  global={attribute.global}
/>

{attribute.values && (
  <>
    ## Possible Values

    <ValueTable
      values={attribute.values}
      type={attribute.valueType}
    />
  </>
)}

## Usage Examples

<CodeExample
  title={`Using the ${attribute.name} attribute`}
  language="html"
  code={attribute.examples?.basic}
/>

{attribute.examples?.multiple && (
  <CodeExample
    title="Multiple values example"
    language="html"
    code={attribute.examples.multiple}
  />
)}

{attribute.examples?.withJavaScript && (
  <CodeExample
    title="Accessing with JavaScript"
    language="javascript"
    code={attribute.examples.withJavaScript}
  />
)}

## Accessibility Considerations

{attribute.accessibility?.map((item, index) => (
  <div key={index} className="accessibility-note">
    <h3>{item.title}</h3>
    <p>{item.description}</p>
  </div>
))}

## Browser Compatibility

<BrowserCompatibility
  feature={`html.global_attributes.${attribute.name}`}
  standard="W3C HTML"
/>

## Specifications

<SpecificationLinks
  specifications={attribute.specifications}
  standard="HTML"
/>

## See Also

- [HTML Living Standard: {attribute.name}](https://html.spec.whatwg.org/multipage/common-microsyntaxes.html#{attribute.name})
- [MDN Web Docs: {attribute.name}](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/{attribute.name})
- [W3C HTML Specification](https://www.w3.org/TR/html52/dom.html#{attribute.name})
