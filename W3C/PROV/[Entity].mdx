---
layout: Entity
source: W3C/PROV
$type: W3C/PROV/Entity
$id: https://standards.org.ai/W3C/PROV/Entity/{entity.code}
sameAs: https://standards.org.ai/Entity/{entity.code}
canonical: https://w3.org.ai/PROV/Entity/{entity.code}
params:
  - Entity
schema:
  entity:
    code: string
    uri: string
    label: string
    type: array
    wasGeneratedBy: object
    wasDerivedFrom: array
    wasAttributedTo: array
    wasInvalidatedBy: object
    generatedAtTime: string
    invalidatedAtTime: string
    value: string
    alternateOf: array
    specializationOf: string
    hadPrimarySource: array
    wasQuotedFrom: array
    wasRevisionOf: array
---

# {entity.label}

<div className="entity-header">
  <div className="entity-metadata">
    <span className="entity-uri">{entity.uri}</span>
    <span className="entity-type">PROV Entity</span>
  </div>
</div>

{entity.type && entity.type.length > 0 && (
  <section className="entity-types">
    <h3>Types</h3>
    <ul>
      {entity.type.map((type, idx) => (
        <li key={idx}><code>{type}</code></li>
      ))}
    </ul>
  </section>
)}

{entity.value && (
  <section className="entity-value">
    <h2>Value</h2>
    <pre><code>{entity.value}</code></pre>
  </section>
)}

<section className="provenance-info">
  <h2>Provenance Information</h2>

  <div className="prov-timeline">
    {entity.generatedAtTime && (
      <div className="prov-event generation">
        <h4>Generated</h4>
        <p className="timestamp">{entity.generatedAtTime}</p>
      </div>
    )}

    {entity.invalidatedAtTime && (
      <div className="prov-event invalidation">
        <h4>Invalidated</h4>
        <p className="timestamp">{entity.invalidatedAtTime}</p>
      </div>
    )}
  </div>
</section>

{entity.wasGeneratedBy && (
  <section className="generation">
    <h3>Was Generated By</h3>
    <div className="activity-card">
      <h4>
        <a href={entity.wasGeneratedBy.uri || entity.wasGeneratedBy}>
          {entity.wasGeneratedBy.label || 'Activity'}
        </a>
      </h4>
      {entity.wasGeneratedBy.type && <p>Type: {entity.wasGeneratedBy.type}</p>}
      {entity.wasGeneratedBy.startedAtTime && (
        <p>Started: {entity.wasGeneratedBy.startedAtTime}</p>
      )}
      {entity.wasGeneratedBy.endedAtTime && (
        <p>Ended: {entity.wasGeneratedBy.endedAtTime}</p>
      )}
    </div>
  </section>
)}

{entity.wasAttributedTo && entity.wasAttributedTo.length > 0 && (
  <section className="attribution">
    <h3>Was Attributed To</h3>
    <ul className="agent-list">
      {entity.wasAttributedTo.map((agent, idx) => (
        <li key={idx}>
          <a href={agent.uri || agent}>{agent.label || agent.name || agent}</a>
          {agent.type && <span className="agent-type"> ({agent.type})</span>}
        </li>
      ))}
    </ul>
  </section>
)}

{entity.wasDerivedFrom && entity.wasDerivedFrom.length > 0 && (
  <section className="derivation">
    <h3>Was Derived From</h3>
    <ul className="entity-list">
      {entity.wasDerivedFrom.map((source, idx) => (
        <li key={idx}>
          <a href={source.uri || source}>{source.label || source}</a>
        </li>
      ))}
    </ul>
  </section>
)}

{(entity.wasRevisionOf || entity.wasQuotedFrom || entity.hadPrimarySource) && (
  <section className="specialized-derivations">
    <h3>Specialized Derivations</h3>

    {entity.wasRevisionOf && entity.wasRevisionOf.length > 0 && (
      <div className="revision">
        <h4>Was Revision Of</h4>
        <ul>
          {entity.wasRevisionOf.map((rev, idx) => (
            <li key={idx}>
              <a href={rev.uri || rev}>{rev.label || rev}</a>
            </li>
          ))}
        </ul>
      </div>
    )}

    {entity.wasQuotedFrom && entity.wasQuotedFrom.length > 0 && (
      <div className="quotation">
        <h4>Was Quoted From</h4>
        <ul>
          {entity.wasQuotedFrom.map((quote, idx) => (
            <li key={idx}>
              <a href={quote.uri || quote}>{quote.label || quote}</a>
            </li>
          ))}
        </ul>
      </div>
    )}

    {entity.hadPrimarySource && entity.hadPrimarySource.length > 0 && (
      <div className="primary-source">
        <h4>Had Primary Source</h4>
        <ul>
          {entity.hadPrimarySource.map((source, idx) => (
            <li key={idx}>
              <a href={source.uri || source}>{source.label || source}</a>
            </li>
          ))}
        </ul>
      </div>
    )}
  </section>
)}

{(entity.alternateOf || entity.specializationOf) && (
  <section className="entity-relations">
    <h3>Entity Relations</h3>

    {entity.specializationOf && (
      <div className="specialization">
        <h4>Specialization Of</h4>
        <p>This entity is a more specific view of: <a href={entity.specializationOf}>{entity.specializationOf}</a></p>
      </div>
    )}

    {entity.alternateOf && entity.alternateOf.length > 0 && (
      <div className="alternates">
        <h4>Alternate Of</h4>
        <p>This entity is an alternate view of:</p>
        <ul>
          {entity.alternateOf.map((alt, idx) => (
            <li key={idx}>
              <a href={alt.uri || alt}>{alt.label || alt}</a>
            </li>
          ))}
        </ul>
      </div>
    )}
  </section>
)}

{entity.wasInvalidatedBy && (
  <section className="invalidation">
    <h3>Was Invalidated By</h3>
    <div className="activity-card">
      <a href={entity.wasInvalidatedBy.uri || entity.wasInvalidatedBy}>
        {entity.wasInvalidatedBy.label || 'Activity'}
      </a>
    </div>
  </section>
)}

<section className="prov-overview">
  <h2>About PROV Entities</h2>
  <div className="info-box">
    <p>
      In PROV, an <strong>Entity</strong> is a physical, digital, conceptual, or other kind of thing
      with some fixed aspects. Entities may be real or imaginary.
    </p>
    <h4>Key Concepts</h4>
    <ul>
      <li><strong>Generation:</strong> Entities are created by activities</li>
      <li><strong>Attribution:</strong> Entities are attributed to agents responsible for them</li>
      <li><strong>Derivation:</strong> Entities can be derived from other entities</li>
      <li><strong>Invalidation:</strong> Entities may cease to exist or become invalid</li>
    </ul>
  </div>
</section>

<section className="rdf-representation">
  <h3>RDF Representation</h3>
  <pre><code>{`@prefix prov: <http://www.w3.org/ns/prov#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

<${entity.uri}> a prov:Entity ;
  rdfs:label "${entity.label}"@en ;${entity.type ? `
  ${entity.type.map(t => `a <${t}>`).join(' ;\n  ')} ;` : ''}${entity.wasGeneratedBy ? `
  prov:wasGeneratedBy <${entity.wasGeneratedBy.uri || entity.wasGeneratedBy}> ;` : ''}${entity.generatedAtTime ? `
  prov:generatedAtTime "${entity.generatedAtTime}"^^xsd:dateTime ;` : ''}${entity.wasAttributedTo ? `
  ${entity.wasAttributedTo.map(a => `prov:wasAttributedTo <${a.uri || a}>`).join(' ;\n  ')} ;` : ''}${entity.wasDerivedFrom ? `
  ${entity.wasDerivedFrom.map(d => `prov:wasDerivedFrom <${d.uri || d}>`).join(' ;\n  ')} ;` : ''}${entity.invalidatedAtTime ? `
  prov:invalidatedAtTime "${entity.invalidatedAtTime}"^^xsd:dateTime ;` : ''}
  .`}</code></pre>
</section>

<section className="sparql-query">
  <h3>SPARQL Query Examples</h3>

  <h4>Trace entity provenance</h4>
  <pre><code>{`PREFIX prov: <http://www.w3.org/ns/prov#>

SELECT ?source ?activity ?agent
WHERE {
  <${entity.uri}> prov:wasDerivedFrom* ?source .
  OPTIONAL { <${entity.uri}> prov:wasGeneratedBy ?activity }
  OPTIONAL { <${entity.uri}> prov:wasAttributedTo ?agent }
}`}</code></pre>

  <h4>Find all derivations</h4>
  <pre><code>{`PREFIX prov: <http://www.w3.org/ns/prov#>

SELECT ?derived
WHERE {
  ?derived prov:wasDerivedFrom <${entity.uri}> .
}`}</code></pre>
</section>
