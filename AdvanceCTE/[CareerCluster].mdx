---
layout: careerCluster
source: AdvanceCTE
$type: AdvanceCTE/CareerCluster
$id: https://standards.org.ai/AdvanceCTE/CareerCluster/{cluster.code}
sameAs: https://standards.org.ai/CareerCluster/{cluster.code}
canonical: https://standards.org.ai/CTE/CareerCluster/{cluster.code}
params:
  - CareerCluster
schema:
  cluster:
    name: careerCluster
---

# {cluster.name}

<CareerClusterHeader name={cluster.name} />

## Sub-Clusters

<SubClusterList cluster={cluster.name} />

## SOC Occupations

<OccupationsByCluster cluster={cluster.name} />

## CIP Programs

<CIPsByCluster cluster={cluster.name} />

## NAICS Industries

<NAICSByCluster cluster={cluster.name} />

## Related Career Clusters

<RelatedCareerClusters cluster={cluster.name} />

## Career Pathways

<CareerPathways cluster={cluster.name} />
